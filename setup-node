# Install curl
```bash
sudo apt install curl
curl --version
```

# nodejs
```bash
	curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash # [nodejs ubuntu nvm link]
	source ~/.bashrc
	nvm install 22
	node -v
	npm -v
```

# pm2
```bash
	npm install -g pm2
	pm2 --version
```
